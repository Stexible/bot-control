# Bot-Control

TUI-менеджер ботов на базе systemd + загрузочное меню для headless-сервера.

> **AI-generated project** — этот проект был полностью написан с помощью ИИ (opencode / big-pickle). Весь код, документация и структура сгенерированы нейросетью.

---

## Описание

Набор инструментов для управления процессами ботов (Telegram, Discord и т.д.) как systemd-сервисами на Linux-сервере без графического интерфейса.

- **`botctl.py`** — полноценное TUI-приложение на Python (curses) для создания, запуска, остановки, настройки и мониторинга ботов
- **`start_choice.sh`** — загрузочное меню, показываемое при входе в TTY1 на сервере без дисплея

## Возможности

| Функция | Горячая клавиша | Описание |
|---|---|---|
| Список всех ботов | — | Таблица с именем, статусом, автозапуском и описанием |
| Запуск / Остановка | `Enter` / `Space` | Переключение состояния сервиса |
| Автозапуск вкл/выкл | `E` | `systemctl enable` / `disable` |
| Просмотр логов | `L` | Последние 300 строк из journalctl |
| Описание бота | `I` | Редактор заметок (Markdown, `/etc/mybots/notes/`) |
| Создать нового бота | `N` | Пошаговый мастер с генерацией unit-файла |
| Adopt существующего | `A` | Взять любой systemd-сервис и превратить в `bot-*` |
| Удалить бота | `X` / `Del` | Остановка, отключение, удаление unit-файла |
| Обновить список | `R` | Перечитать сервисы из systemd |
| CLI-отчёт | `--report` | Цветной статус для SSH/скриптов |

## Требования

- Linux с **systemd**
- **Python 3** (только stdlib, без зависимостей)
- **root/sudo** (для управления сервисами)

## Установка

```bash
# Клонировать репозиторий
git clone https://github.com/stexible/bot-control.git
cd Bot-Control

# (Опционально) Создать симлинк для удобства
sudo ln -s "$(pwd)/BOTS/botctl.py" /usr/local/bin/botctl
```

## Использование

```bash
# TUI-менеджер (нужен root)
sudo python3 BOTS/botctl.py
# или через симлинк:
sudo botctl

# CLI-отчёт (работает без root)
python3 BOTS/botctl.py --report
python3 BOTS/botctl.py --report --no-color

# Загрузочное меню (при входе на TTY1 без $DISPLAY)
./start_choice.sh
```

## Структура

```
Bot-Control/
├── start_choice.sh       # Загрузочное меню (Bash)
├── README.md
└── BOTS/
    └── botctl.py         # TUI + CLI менеджер (Python 3)
```

## Как это работает

`botctl.py` управляет systemd-сервисами с префиксом `bot-` (например, `bot-telegram.service`). Все сервисы группируются в `bots.target`. Заметки для каждого бота хранятся в `/etc/mybots/notes/<имя>.md`.

---

# Bot-Control

TUI service manager for bots based on systemd + boot menu for headless servers.

> **AI-generated project** — this project was fully written with the help of AI (opencode / big-pickle). All code, documentation, and structure were generated by a neural network.

---

## Description

A toolkit for managing bot processes (Telegram, Discord, etc.) as systemd services on a headless Linux server.

- **`botctl.py`** — a full-featured TUI application built with Python curses for creating, starting, stopping, configuring, and monitoring bots
- **`start_choice.sh`** — a boot menu displayed at TTY1 login on displayless servers

## Features

| Feature | Hotkey | Description |
|---|---|---|
| List all bots | — | Table with name, status, auto-start, and description |
| Start / Stop | `Enter` / `Space` | Toggle service state |
| Toggle auto-start | `E` | `systemctl enable` / `disable` |
| View logs | `L` | Last 300 lines from journalctl |
| Bot description | `I` | Markdown notes editor (`/etc/mybots/notes/`) |
| Create new bot | `N` | Step-by-step wizard with unit file generation |
| Adopt existing | `A` | Take any systemd service and turn it into a `bot-*` service |
| Delete bot | `X` / `Del` | Stop, disable, and remove unit file |
| Refresh list | `R` | Re-read services from systemd |
| CLI report | `--report` | Colored status for SSH / scripts |

## Requirements

- Linux with **systemd**
- **Python 3** (stdlib only, zero dependencies)
- **root/sudo** (for service management)

## Installation

```bash
git clone https://github.com/stexible/bot-control.git
cd Bot-Control

# (Optional) Create a symlink for convenience
sudo ln -s "$(pwd)/BOTS/botctl.py" /usr/local/bin/botctl
```

## Usage

```bash
# TUI manager (requires root)
sudo python3 BOTS/botctl.py
# or via symlink:
sudo botctl

# CLI status report (works without root)
python3 BOTS/botctl.py --report
python3 BOTS/botctl.py --report --no-color

# Boot menu (on TTY1 with no $DISPLAY)
./start_choice.sh
```

## Structure

```
Bot-Control/
├── start_choice.sh       # Boot menu (Bash)
├── README.md
└── BOTS/
    └── botctl.py         # TUI + CLI manager (Python 3)
```

## How it works

`botctl.py` manages systemd services with the `bot-` prefix (e.g., `bot-telegram.service`). All services are grouped under `bots.target`. Notes for each bot are stored in `/etc/mybots/notes/<name>.md`.

## License

MIT
